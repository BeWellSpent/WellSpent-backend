# Copy to .env.db (gitignored) on whichever machine hosts docker-compose.db.yml.
# Not SOPS-encrypted like .env.dev/.env.prod — this only needs to exist on the
# one host machine, not travel through git to every developer.
POSTGRES_USER=wellspent
POSTGRES_PASSWORD=change-me
POSTGRES_DB=wellspent_dev
