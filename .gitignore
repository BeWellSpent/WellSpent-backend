# Binaries
bin/
tmp/
*.exe

# Go build cache
*.test

# Environment — plaintext files are never committed
# Commit the .enc equivalents instead (encrypted with SOPS + age)
.env.dev
.env.uat
.env.prod
!.env.dev.enc
!.env.uat.enc
!.env.prod.enc

# Generated code (regenerate with `make generate`)
gen/

# OS
.DS_Store
Thumbs.db
